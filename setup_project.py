import os

base_dir = "resume-tailor-mvp"

files = {
    f"{base_dir}/README.md": """# Resume Tailor MVP

🚀 A FastAPI-based backend for customizing resumes with AI.

## Run Locally

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```
""",
}
